# What is Directory Enumeration?

When we visit a website, we see pages like `example.com/home` or `example.com/about` But websites often have hidden directories and files that aren't linked anywhere - things like admin panels, backup files, test pages, or configurations files.
